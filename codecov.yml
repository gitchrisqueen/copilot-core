coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 0.5%
    patch:
      default:
        target: 80%
flags:
  js:
    paths:
      - js/
  python:
    paths:
      - src/copilot_core/
comment:
  layout: "condensed_header, diff, flags"
